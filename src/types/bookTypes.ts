export interface IBook {
  id: string;
  img: string;
  author: string;
  title: string;
  description: string;
  price: string;
  category: string;
}

export interface ICategory {
  category: string;
}
